services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pond_planner_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
